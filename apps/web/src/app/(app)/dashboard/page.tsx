/** Dashboard placeholder (Phase 3 fills with real stats from the api). */
export default function DashboardPage() {
  return (
    <main className="container py-16">
      <h1 className="text-3xl font-semibold">Dashboard</h1>
      <p className="mt-3 text-muted-foreground">
        Your conversations, talk time, remaining minutes, mood score, and weekly summary will
        appear here.
      </p>
    </main>
  );
}
