import { Module } from '@nestjs/common';

/**
 * Conversations module (Phase 2/3): persistence + history APIs for the
 * `conversations` collection and its `messages` subcollection.
 */
@Module({})
export class ConversationsModule {}
