import { Module } from '@nestjs/common';

/**
 * Billing module (Phase 4): Stripe subscriptions + PAYG credit packs, checkout
 * sessions, and signature-verified webhooks. Plans/pricing are env-driven.
 */
@Module({})
export class BillingModule {}
